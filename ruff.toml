line-length = 100
target-version = "py310"

[lint]
# Critical-only gate: syntax errors and pyflakes runtime-error classes.
# Style-level rules can be tightened later without touching the contract.
select = ["E9", "F63", "F7", "F82"]
